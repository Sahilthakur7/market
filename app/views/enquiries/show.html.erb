<strong><%= @enquiry.player.agent.name%>:</strong>
</br>

<%= @enquiry.response%>
</br>
</br>

<%= simple_form_for([@player,@enquiry]) do |f|%>
    <%= f.input :seen%>
    <%=f.button :submit, "Select", class: "btn btn-small"%>
<%end%>
</br>

<%= link_to "Transfer", new_player_transfer_path(@player), class: "btn btn-success"%>
